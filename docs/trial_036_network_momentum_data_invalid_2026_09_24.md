# Trial 036 — DATA_INVALID — Cross-Asset Network Momentum

## Status

**DATA_INVALID / NO_SCIENTIFIC_OUTCOME**

Der Preflight wurde vor jeder Performanceauswertung beendet.

## Nachweis

- Coverage-Workflow: `36049739991`
- Fehler: Yahoo HTTP 404 für `CYB`
- Performanceauswertung: nicht durchgeführt
- Holdout-Auswertung: nicht durchgeführt
- kein Candidate-Status erzeugt
- kein Datenvertrag gelockert
- keine Holdout-Auswahl
- keine Orders

## Konsequenz

Trial 036 wird ausschließlich als Datenqualitätsereignis archiviert.

Die Hypothese bleibt wissenschaftlich **ungetestet**. Es wird kein negatives oder positives
Performanceurteil aus dem fehlgeschlagenen Preflight abgeleitet.

Für die nächste Coverage-Prüfung wird ausschließlich `CYB` durch `EIDO` ersetzt;
alle übrigen Symbole und die präregistrierte Network-Momentum-Regel bleiben unverändert.
