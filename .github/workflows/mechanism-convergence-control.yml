name: Mechanism Convergence Control

on:
  pull_request:
    types:
      - opened
      - synchronize
    branches:
      - master
    paths:
      - "automation/mechanism_convergence_control.py"
      - "tests/test_mechanism_convergence_control.py"
      - ".github/workflows/mechanism-convergence-control.yml"
      - "automation/cross_asset_trend_replication.py"
      - "automation/cross_sectional_momentum_replication.py"
      - "automation/prepare_research_data.py"
      - "research/asset_universes.py"
      - "data/yahoo_loader.py"
  workflow_dispatch:

permissions:
  contents: read
  actions: read

concurrency:
  group: trading-agent-mechanism-convergence-control
  cancel-in-progress: true

jobs:
  convergence:
    runs-on: ubuntu-24.04-arm
    timeout-minutes: 240

    steps:
      - name: Repository auschecken
        uses: actions/checkout@v4

      - name: Python einrichten
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Abhängigkeiten installieren
        run: |
          python -m pip install --upgrade pip
          python -m pip install pytest

      - name: Vollständige Testsuite
        run: python -m pytest -q

      - name: Paper-Trading-Sicherheit prüfen
        run: |
          set -euo pipefail
          python - <<'PY'
          from config import settings

          assert settings.PAPER_ONLY is True
          assert settings.LIVE_TRADING_ENABLED is False
          print("PAPER-ONLY SAFETY OK")
          PY

      - name: Cross-Asset-Trenddaten vorbereiten
        run: |
          set -euo pipefail
          python -m automation.prepare_research_data             --target-count 3500             --universe cross_asset_trend             --output research/mechanism_convergence/trend_manifest.json

      - name: Cross-Sectional-Momentumdaten vorbereiten
        run: |
          set -euo pipefail
          python -m automation.prepare_research_data             --target-count 1300             --universe liquid_high_volatility             --output research/mechanism_convergence/cs_manifest.json

      - name: Datenmanifest prüfen
        run: |
          set -euo pipefail
          python - <<'PY'
          import json
          from pathlib import Path

          trend = json.loads(
              Path("research/mechanism_convergence/trend_manifest.json")
              .read_text(encoding="utf-8")
          )
          cross_sectional = json.loads(
              Path("research/mechanism_convergence/cs_manifest.json")
              .read_text(encoding="utf-8")
          )

          assert trend["universe"] == "cross_asset_trend"
          assert trend["target_count"] == 3500
          assert len(trend["datasets"]) == 8

          assert cross_sectional["universe"] == "liquid_high_volatility"
          assert cross_sectional["target_count"] == 1300
          assert len(cross_sectional["datasets"]) == 5

          assert trend["safety"]["paper_only"] is True
          assert trend["safety"]["live_trading_enabled"] is False
          assert cross_sectional["safety"]["paper_only"] is True
          assert cross_sectional["safety"]["live_trading_enabled"] is False

          print("MECHANISM CONVERGENCE MANIFESTS VERIFIED")
          print("TREND_MANIFEST:", trend["manifest_fingerprint"])
          print("CS_MANIFEST:", cross_sectional["manifest_fingerprint"])
          PY

      - name: Mechanismus-Konvergenz-Control ausführen
        run: |
          set -euo pipefail
          python -m automation.mechanism_convergence_control             --trend-data-dir data/market_data             --trend-manifest research/mechanism_convergence/trend_manifest.json             --cs-data-dir data/market_data             --cs-manifest research/mechanism_convergence/cs_manifest.json             --output research/mechanism_convergence/report.json

      - name: Ergebnis-Fingerprint und Methodik prüfen
        run: |
          set -euo pipefail
          python - <<'PY'
          import hashlib
          import json
          from pathlib import Path

          path = Path("research/mechanism_convergence/report.json")
          report = json.loads(path.read_text(encoding="utf-8"))

          stored = report.pop("report_fingerprint")
          actual = hashlib.sha256(
              json.dumps(
                  report,
                  sort_keys=True,
                  separators=(",", ":"),
                  ensure_ascii=True,
                  allow_nan=False,
              ).encode("utf-8")
          ).hexdigest()

          assert stored == actual
          assert report["status"] == "COMPLETED"
          assert report["methodology"]["optimization_used"] is False
          assert report["methodology"]["selection_profile_used"] is False
          assert report["methodology"]["no_new_signal_tuning"] is True
          assert report["methodology"]["holdout_is_blind_to_selection"] is True
          assert report["safety"]["paper_only"] is True
          assert report["safety"]["live_trading_enabled"] is False
          assert report["safety"]["orders_enabled"] is False

          print("REPORT_FINGERPRINT: VALID")
          print("REPORT_FINGERPRINT:", stored)
          PY

      - name: Konvergenz-Artefakte sichern
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: mechanism-convergence-control-${{ github.run_id }}
          path: |
            research/mechanism_convergence/
            data/market_data/
          if-no-files-found: error
          retention-days: 30
